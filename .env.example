# Mori Campus Environment Variables Example
# Since this is a client-side static 3D web application, no backend secrets are needed.
# These variables can be used at build time to configure Vite.

# Example variable (optional)
VITE_APP_TITLE="Mori Campus"
